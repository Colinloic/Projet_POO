{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
     <div class="border-yellow
                border-blue
                border-dark
                border-white
                border-primary
                border-water
                border-dragon
                border-electrik
                border-fight
                border-bug
                border-fire
                border-fly
                border-ghost
                border-grass
                border-ground
                border-ice
                border-normal
                border-poison
                border-psy
                border-rock"></div>

               
 {% for key, pokemon in pokemons %} 
    
        <div class=" {{ 'border-'~pokemon.category.url }} flex justify-center items-center bg-black bg-opacity-50 rounded-2xl h-full w-full relative p-8 z-30">
                                    <p class="flex absolute left-0 bottom-0  mx-auto font-medium bg-black bg-opacity-50 text-gray-200 text-sm px-5 py-2 rounded-2xl"> Auteur: {{ pokemon.author }}</p>

                <div class="absolute h-full w-full">
                    <p class="absolute w-full right-0 font-medium bg-black bg-opacity-50 text-gray-200 px-5 py-2 rounded-t-2xl text-center uppercase">{{ pokemon.name }}</p>
                    <a href="{{ path('auth_login') }}" class="flex absolute right-0 bottom-0 mx-auto font-medium bg-black bg-opacity-50 text-gray-200 text-sm px-5 py-2 rounded-tl-2xl rounded-br-2xl">Plus d'informations</a>
                </div>
                 <img src="{{ asset('static/pokemons/'~pokemon.imageurl) }}" alt="" class="object-contain" height="230" width="230"">
    
            </div>
            

    {% endfor %}
 

{% endblock %}
